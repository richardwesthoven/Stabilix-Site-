import React from "react";
import ReactDOM from "react-dom/client";
import AerFrameLanding from "./App";
import "./styles/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <AerFrameLanding />
  </React.StrictMode>
);
